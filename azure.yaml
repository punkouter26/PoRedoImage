# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoRedoImage
metadata:
  template: PoRedoImage@0.0.1-beta
services:
  api:
    project: ./Server
    language: dotnet
    host: appservice
infra:
  provider: bicep

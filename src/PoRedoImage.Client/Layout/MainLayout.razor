@inherits LayoutComponentBase

@* Client WASM layout — minimal passthrough.
   The full app shell (header, nav, footer) is rendered by the SSR host
   (PoRedoImage.Web Components/Layout/MainLayout.razor). *@
<article class="content px-4 py-4">
    @Body
</article>

<RadzenComponents />
